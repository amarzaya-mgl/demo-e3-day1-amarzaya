const b=document.querySelector('.menu-btn'),n=document.querySelector('.nav');b?.addEventListener('click',()=>{let o=n.classList.toggle('open');b.setAttribute('aria-expanded',o);b.textContent=o?'×':'☰'});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.querySelector('#contact-form')?.addEventListener('submit',e=>{e.preventDefault();e.target.querySelector('.form-status').textContent='Баярлалаа. Таны хүсэлтийг хүлээн авлаа.';e.target.reset()});
